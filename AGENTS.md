# Agent notes

- Prefer additive changes that preserve existing content and behaviour.
- Do not rewrite published git history (force push, rebase, amend, or squash of already-pushed commits) unless the user explicitly requests it.
- Keep the connected branch in a working state when pushing.
